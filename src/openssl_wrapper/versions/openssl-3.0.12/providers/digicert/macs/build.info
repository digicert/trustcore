# We make separate GOAL variables for each algorithm, to make it easy to
# switch each to the Legacy provider when needed.

$HMAC_GOAL=../../libdigiprov.a
$CMAC_GOAL=../../libdigiprov.a
$BLAKE2_GOAL=../../libdigiprov.a
$POLY1305_GOAL=../../libdigiprov.a

SOURCE[$HMAC_GOAL]=digi_hmac.c

IF[{- !$disabled{cmac} -}]
  SOURCE[$CMAC_GOAL]=digi_cmac.c
ENDIF

IF[{- !$disabled{blake2} -}]
  SOURCE[$BLAKE2_GOAL]=digi_blake2s_mac.c digi_blake2b_mac.c
ENDIF

IF[{- !$disabled{poly1305} -}]
  SOURCE[$POLY1305_GOAL]=digi_poly1305.c
ENDIF
