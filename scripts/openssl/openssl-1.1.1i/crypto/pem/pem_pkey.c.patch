24a25,28
> #if defined(__ENABLE_DIGICERT_SSL_PEM_READ_BIO_REDEFINE__)
> EVP_PKEY *PEM_read_bio_PrivateKey_ex(BIO *bp, EVP_PKEY **x, pem_password_cb *cb,
>                                   void *u)
> #else
26a31
> #endif
93a99,119
> #if defined(__ENABLE_DIGICERT_SSL_PEM_READ_BIO_REDEFINE__)
> extern EVP_PKEY *(*PEM_read_bio_PrivateKey_handler)(BIO *, EVP_PKEY**, pem_password_cb *, void *);
> 
> EVP_PKEY *PEM_read_bio_PrivateKey(BIO *bp, EVP_PKEY **x, pem_password_cb *cb,
>                                   void *u)
> {
>     EVP_PKEY *pKey = NULL;
>     if (PEM_read_bio_PrivateKey_handler != NULL)
>     {
>         /* Try to call the handler if available. If it fails then call the
>          * original implementation.
>          */
>         pKey = PEM_read_bio_PrivateKey_handler(bp, x, cb, u);
>         if (NULL != pKey)
>             return pKey;
>     }
> 
>     return PEM_read_bio_PrivateKey_ex(bp, x, cb, u);
> 
> }
> #endif
