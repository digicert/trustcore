name: "Build_NanoSSL_FIPS"

permissions:
  contents: read

on:
  workflow_dispatch: {}
  schedule:
    - cron: '30 15 * * *'  # Every day at 9:00 PM IST (3:30 PM UTC)

jobs:
  build-nanossl-fips-test:
    runs-on: [digicert-medium]

    steps:
      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y cmake build-essential yasm
          cmake --version

      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Build FIPS Crypto Library
        id: fips_lib_build
        continue-on-error: true
        run: |
          cd projects/crypto_fips
          ./MAKE_ALL_UB24_ESV.sh

      - name: Install FIPS Crypto Library
        id: fips_lib_install
        continue-on-error: true
        run: |
          sudo cp projects/crypto_fips/x86_64/libmss.so     /usr/local/lib/
          sudo cp projects/crypto_fips/x86_64/libmss.so.sig /usr/local/lib/
          sudo ldconfig
          cp -r projects/crypto_fips/x86_64/libmss.so lib/
          cp -r projects/crypto_fips/x86_64/libmss.so.sig lib/

      # ---------------- NanoSSL FIPS builds ----------------

      - name: Build ssl_server (FIPS)
        id: ssl_server_fips
        continue-on-error: true
        run: |
          rm -f samples/bin/ssl_server
          ./scripts/nanossl/ssl_server/build_ssl_server_cap.sh --gdb --debug --fips
          test -f samples/bin/ssl_server

      - name: Build ssl_client (FIPS)
        id: ssl_client_fips
        continue-on-error: true
        run: |
          rm -f samples/bin/ssl_client
          ./scripts/nanossl/ssl_client/build_ssl_client_cap.sh --gdb --debug --fips
          test -f samples/bin/ssl_client

      - name: Build ssl_client_sp800_135
        id: ssl_sp800_135_build
        continue-on-error: true
        run: |
          rm -f samples/bin/ssl_client_sp800_135
          ./scripts/nanossl/ssl_client/build_ssl_client_cap.sh --gdb --debug --sp800-135
          test -f samples/bin/ssl_client_sp800_135

      - name: Run SP800-135 KDF test (sampleUsageSSL status = 0)
        id: ssl_sp800_135_kdf
        continue-on-error: true
        run: |
          test -f samples/bin/ssl_client_sp800_135
          LD_LIBRARY_PATH="$PWD/lib:$LD_LIBRARY_PATH" ./samples/bin/ssl_client_sp800_135
          echo "sampleUsageSSL returned status 0 (KDF PASS)"

      - name: Build summary
        if: always()
        run: |
          FAILED=0
          if [[ "${{ steps.fips_lib_build.outcome }}" == "failure" ]]; then
            echo "❌ Build FIPS Crypto Library FAILED"
            FAILED=1
          fi
          if [[ "${{ steps.fips_lib_install.outcome }}" == "failure" ]]; then
            echo "❌ Install FIPS Crypto Library FAILED"
            FAILED=1
          fi
          if [[ "${{ steps.ssl_server_fips.outcome }}" == "failure" ]]; then
            echo "❌ Build ssl_server (FIPS) FAILED"
            FAILED=1
          fi
          if [[ "${{ steps.ssl_client_fips.outcome }}" == "failure" ]]; then
            echo "❌ Build ssl_client (FIPS) FAILED"
            FAILED=1
          fi
          if [[ "${{ steps.ssl_sp800_135_build.outcome }}" == "failure" ]]; then
            echo "❌ Build ssl_client_sp800_135 FAILED"
            FAILED=1
          fi
          if [[ "${{ steps.ssl_sp800_135_kdf.outcome }}" == "failure" ]]; then
            echo "❌ SP800-135 KDF test (sampleUsageSSL status = 0) FAILED"
            FAILED=1
          fi

          if [[ $FAILED -eq 1 ]]; then
            echo ""
            echo "One or more NanoSSL FIPS builds/tests failed."
            exit 1
          else
            echo ""
            echo "All NanoSSL FIPS builds and KDF test passed."
          fi

      - name: Get Slack User ID
        if: always()
        id: slack_map
        run: |
          GITHUB_USER="${{ github.actor }}"
          SLACK_USER_MAP='${{ vars.SLACK_USER_MAP_JSON }}'

          SLACK_USER_ID=$(echo "$SLACK_USER_MAP" | jq -r --arg user "$GITHUB_USER" '.[$user] // ""')

          echo "GitHub User: $GITHUB_USER"
          echo "slack_user_id=$SLACK_USER_ID" >> $GITHUB_OUTPUT

      - name: Send Slack notification (Failure)
        if: always() && (failure() || cancelled())
        run: |
          SLACK_USER_ID="${{ steps.slack_map.outputs.slack_user_id }}"
          if [ -n "$SLACK_USER_ID" ]; then
            MENTION="<@$SLACK_USER_ID>"
          else
            MENTION="${GITHUB_ACTOR}"
          fi
          if [ "$GITHUB_REF_NAME" = "main" ]; then
            MESSAGE="Build NanoSSL FIPS failed on branch \`${GITHUB_REF_NAME}\`\nRun: https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          else
            MESSAGE="Build NanoSSL FIPS failed on branch \`${GITHUB_REF_NAME}\`\nTriggered by: ${MENTION}\nRun: https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          fi
          curl -X POST -H 'Content-type: application/json' --data "{\"text\": \"${MESSAGE}\"}" ${{ secrets.SLACK_WEBHOOK_URL }}
